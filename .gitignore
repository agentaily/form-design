node_modules
dist
*.local
.DS_Store

# test artifacts
coverage
playwright-report
test-results
.playwright

# design-sync baseline (working snapshot of the last Claude Design handoff)
.design-baseline

# wrangler 本地 dev 密钥（throwaway，绝不进 git）
workers/.dev.vars
.dev.vars

# wrangler 本地状态（miniflare D1/KV、临时 bundle）—— 绝不进 git
.wrangler/
workers/.wrangler/
