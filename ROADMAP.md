# Agentaily Forms — Roadmap

> 对话式动态表单设计器：**设计 → 发布 → 收集 → 看结果**。
> 产品规格见 [SPEC.md](./SPEC.md)，运维见 [OPERATIONS.md](./OPERATIONS.md)，
> 开发文档（研发驱动范式）见 [DEVELOPMENT.md](./DEVELOPMENT.md)。

---

## ✅ 已完成

### 前端（`src/`，已上线 https://form-design.agentaily.com）

- 对话式设计器：左 Agent 对话 + 右实时表单预览
- **API client 层**（`core/apiClient` + `core/sse`）：fetch 封装 + `VITE_API_BASE` + Bearer token 注入 + SSE 流解析
- **对话接真后端 `POST /api/chat`**：DeepSeek 流式（OpenAI 协议）替换写死脚本；`core/designerLoop` 单回合 ReAct（自愈）+ `core/designerTools`（UI 字段模型工具：set_meta / add / update / remove / duplicate / reorder）；对话引擎对测试可注入
- 表单工具：`add` / `update` / `remove` / `duplicate` / `reorder` field + 校验
- **markup 指向修改**：hover 高亮预览元素、点击带身份发消息到对话（DS `MarkupLayer`，靠 preview 上的 `data-mk-*`）
- **连续发送（缓冲区）**：处理中可继续输入，消息收进缓冲区、下一轮一次性合并处理（SPEC §4.1，`core/queue.ts`；缓冲区 UI 由 DS `ConversationThread` 的 `controller` 渲染）
- **表单校验走 DS `Form.useForm`**：按类型规则（必填 + 邮箱 + 11 位手机号）、提交即定位首个错误、实时纠错（依赖 `@agentaily/design-system` 0.2.0）
- **响应式适配**：≤720px 单列 + 对话/预览分段切换器、头部压缩、分享收成图标；≤380px 仅留 logo
- **设计系统组件全面上移（DS 0.2.0 → 0.6.0，2026-06-12；含两轮向 DS 反馈 seam）**：把本地手搓的页面外壳 / 对话线程 / 账户控件 / 登录 / 集成设置 / 指向修改全部换成上游 DS 组件——`DesignerShell`（双栏外壳 + 拖拽 + 移动端切换）、`ConversationThread`（纯渲染 + `controller`，对接现有 §4.1 `core/queue.ts`，富消息用 `renderTurn` 配 `Reasoning/ToolCall/Suggestions/Message`）、`AccountControl`（账户下拉：我的表单 / 集成设置 / 退出登录）、`SignInPage`（**登录改独立 `/signin` 路由页**，原 `LoginDialog` 弹窗删除；受限操作未登录 → 跳登录页 + 回跳续跑，intent 经 sessionStorage 跨页）、`MarkupLayer`、统一 `Icon`、布局 token `--bar-h/--topbar-h`。删 `src/auth.jsx`/`src/markup.jsx` + 整段手写外壳 / 线程 / markup 的 CSS（app.css 1028→约 560 行）。**集成设置改为独立 `/settings` 路由页**（`src/settings.jsx` = `SettingsScreen`）：DS 纯展示连接卡 `DeepSeekCard` + `FeishuCard` + form-design 自己的保存栏 / **后端 400 逐字回显** / 门禁，接真 BYOK 后端 `core/configClient`（掩码不重交 §12.4、401→/signin、逐连接测试），删 `SettingsDialog`；**`SignInPage` 用上游 `error`/`submitting` seam**、logo 字标光标随 DS 默认（`BrandMark` cursor=false）消失。这两处经**向 DS 反馈两轮 seam**落地（0.5.0：SignInPage error/submitting + IntegrationSettings 受控 seam；0.6.0：集成设置**拆成纯展示连接卡**、删旧 `IntegrationSettings`、BrandMark 默认去光标）。全程经 **claude.ai/design 对话设计 → `design-sync` 落地**，不再手搓（沉淀为全局 skill `design-via-claude-design`；上游发版 + 破坏性变更下游通知流程见 design-system 的 `RELEASE.md`）。
- **DS 升级 0.6 → 0.10（随上游发版流过来，design-sync 落地）**：0.7.0 聊天气泡接 DS `<Markdown>` 渲染（PR #49）；**0.8.0 集成设置区重构为浮层组件链**（PR #51，handoff `IAZjvUx78` + 上游 0.8.0 breaking）——`SettingsPage`（已删）→ `SettingsSheet`（浮层 settings 页骨架）› `IntegrationSettings`（集成分区:hero + 就绪栏 + 连接卡槽，children 模式塞真实接线的 `DeepSeekCard`/`FeishuCard`）+ 底部 `SettingsSaveBar`（显式保存,GitHub 模型,接现有 dirty/save/400 显示）。仍是 **`/settings` 路由 + 真实 BYOK 后端 + 401→/signin** 不变；删 `showUsageCap` 用法、`.s-*`→`.ax-*` 由 DS 内部收敛。#51 范围内只做集成区。**0.9.0 补 `monitor` 图标（+ 补 `IconName` 漏列的 `menu`），桌面切换按钮把 #52 临时内联的 monitor SVG 换回 `<Icon name="monitor">`（PR #54）——monitor 尾巴闭环、不再手搓 glyph。** **0.10.0 集成区改「调用方自组合」（PR #55，上游 0.10.0 breaking）**——上游删 `IntegrationSettings`/`FeishuCard`（连同 `parseFeishuLink`）+ `DeepSeekCard` 去掉对话模型选择;`src/settings.jsx` 改为自己用通用 `ConnectionCard`+`SecretField`+`HelpSteps` 组合 DeepSeek + 飞书两张对称卡 + **自实现就绪栏只 gate DeepSeek（飞书可选，chat13）**，`parseFeishuLink` 内联到产品侧（link→appToken/tableId 桥接不变、后端契约零回退）；砍集成区模型选择 UI（`model` 仍 round-trip 不被清），**对话级模型芯片不在本根范围**。飞书卡 link-less + 发布自动建表是 **PR-4**（前置 PR-1+PR-3），本根仍保留分享链接字段。
- **设置浮层 + 账户 tab（显示名）+ profile 后端 + chrome 逐组件对齐（PR #52，承接 #51，handoff `IAZjvUx78` 完整版）**：把设置从 `/settings` 路由页改为**设计器内浮起浮层**（`src/settings.jsx` → `SettingsOverlay`，DS `SettingsSheet` 双 tab：账户 + 集成），叠在设计器上**不卸载**；打开经 `history.pushState` **反映 `/settings` URL**、✕/Esc/浏览器后退关闭并复原（**route-reflected overlay，非删路由**）。新增**账户 tab**：头像/邮箱 + 可编辑**显示名** + 退出登录，显示名走**真实 profile 后端**（新 `PUT /api/auth/profile` + `GET /api/auth/me` 返回 `displayName` + D1 迁移 `0004_owner_display_name.sql` 给 `users` 加 `display_name`，owner-only，§17.13）——**非 localStorage 假桩**。逐组件对齐 handoff 的 chrome：账户下拉「集成设置」改插头图标 + 排在「我的表单」之上、顶栏加 `BrandMark` + 面包屑左对齐、桌面切换换显示器(monitor)图标。`core/auth.ts` 加 `updateProfile` + `CurrentUser.displayName`。**合并 + 部署（含 D1 迁移上生产，不可逆）人点。**
- **「我的表单」面板 Dialog → PanelSheet + 卡片操作收敛（PR #58 / 内部根 PR-5，handoff `BJ0OUqPwIW-dZwObPNkcnQ`，承接 #55 的 DS 0.10.0 基线）**：`src/forms-panel.jsx` 的 `FormsPanel` 从 DS `Dialog` 迁到**满宽顶栏 `PanelSheet`（`barFullWidth`）** + `PageSection` + **表单卡片网格**（每份表单一张 DS `Card`：公开链接/状态徽标 + 标题 + 创建时间 + 「查看全部提交」计数式入口）。**卡片底部操作收敛**：`复制链接` + `编辑`（占位禁用，真「载回设计器编辑」流见 **PR-7 / #62**）+ `⋯` 溢出菜单（DS `DropdownMenu`：`关闭收集`/`重新发布` 走现有 PATCH、`删除` 走 DS `AlertDialog` 二次确认 + 现有 DELETE）。「查看全部提交」**只换计数样式、点击仍开现有 `SubmissionsView`**——面板内 list↔提交↔详情切换 + 读 D1 计数是 **PR-6（依赖 #56）**，本根不做。行为契约（`form-publish-mgmt.feature`：发布/列表/改状态/删除确认/401→登录）不变,集成测试改为开 `⋯` 菜单点 menuitem 来 realize。`PublishFeedback` 仍用 `Dialog`（非本根范围）。**合并 + 部署人点。**
- **提交数据面板内子页切换 + 接 D1 + 修 F2（PR #60 / 内部根 PR-6，handoff `BJ0OUqPwIW-dZwObPNkcnQ` chat13，依赖 #56+#58）**：点表单卡的「查看全部提交」不再开独立 `Dialog`，而是在**同一块 `PanelSheet` 内** swap 内容(`src/submissions-view.jsx` 由 `SubmissionsView`(Dialog) 改为 **content-only `SubmissionsContent`**，由 `forms-panel.jsx` 的 `FormsPanel` 内联托管 list↔提交数据↔记录详情三态)。面包屑 我的表单→提交数据→#记录号 各级可回退 + `←` 返回;**动效**:PanelSheet **首开有上浮动画**、内部切换**无面板级动效**(只内层 `.mf-swap` 换 key 做内容 fade、PanelSheet 不 remount)、**记录详情是面板内子页非 Dialog**。**接 #56 的 D1 数据**:提交列表/详情/统计(累计/今日/近7天/完成率=平均字段填充率)/CSV 全基于真实 D1 投影;列由各条提交的作答标签并集推导(D1 不带表单 schema)。**★ 修 F2**(#56 reviewer 留的跨根尾巴):#56 把响应 shape 翻成 D1 形状,对齐前端三处——`src/core/submissionsClient.ts`(`Submission`:`{id,answers:[{label,value}],createdAt,feishu:{recordId,syncedAt,error}}`,去掉 409/502 文档)、`src/core/publicClient.ts`(`SubmitResult`:`{ok,id}`,去掉未配飞书 409/上游 502)、`submissions-view.jsx`(字段访问从旧 `recordId/fields/createdTime` 改读 `id/answers/createdAt`)——否则「查看全部提交」运行时读不到 D1 数据。`data-dashboard.feature` 翻到 D1 真相(去 409/502 场景 + 加面板内详情子页场景),集成测试改为渲染真实 `FormsPanel` 经真入口驱动。done-gate 全绿(typecheck + 前端 654 + build + workers 430 + e2e 7)。**上游 DS 反馈待人转**:`DataTable` 把分页脚/表头放进横向滚动容器、横滚会漂移,本根用作用域 `.sb-tablewrap` sticky 兜底,根因该回 DS 修。**合并 + 部署人点。**
- **表单编辑入口 · 载回设计器继续编辑（PR #62 / 内部根 PR-7，handoff `BJ0OUqPwIW-dZwObPNkcnQ` chat12+chat13，依赖 #55+#58）**：把 #58 卡片上占位禁用的「继续编辑 / 编辑」接真——点它经 `formsClient.getFormForEdit(slug)` 把已发布 / 已关闭表单的 **meta + 字段 schema 载回设计器**（`App.jsx` `loadFormForEdit`），进入**编辑态**。**载回靠 owner 读回**：列表 summary 不含 fields、且无 owner GET，故用**空体 no-op `PATCH`** 读回完整 owner 视图（含 `status`、owner-scoped 跨 owner→404），再 wire→UI 反向映射（`text/radio/select/checkbox`→`text/radio/select/checks`；**tel/email/textarea/consent 细分按既有 wire 契约不可逆退化**，已在 feature 注明）。**字段 id 全程保留**（`reserveUidsFrom` 把会话 uid 计数器推过载入 id 防新字段撞），写回时 PATCH 原 id → 后端按 id 认出改标签 = **飞书列改名**（而非删旧建新孤立列丢数据）。**编辑横幅**（`src/app.css` `.d-editbar`）：低调发丝线 `■ EDITING` mono 标签 + 状态点 + 一行文案 + 「详情」`HoverCard` + 「退出」，与下方工具栏**等高**（`--bar-h`）；**状态自洽**——已发布走「线上仍在收集」、已关闭走「未在收集」+ 顶栏徽章随真实 `status`（已关闭绝不显 LIVE）。顶栏主按钮编辑态由「发布」变「**更新**」→ `formsClient.updateFormDefinition(slug, meta, fields)` 走现有 `PATCH` 整块写回（无改动灰着）。**放弃保护**：`editDirty`（meta+字段快照对比、忽略 id/`_new`）脏时退出弹 DS `AlertDialog`「放弃本次编辑？」二次确认（继续编辑 / 放弃改动），干净时直接退。**顺带修** `FormPreview` 在 `meta.meta`（可选要点标签）缺失时崩溃的潜在 bug。新契约 `features/form-editing.feature` + 单测（`getFormForEdit`/`updateFormDefinition`/`reserveUidsFrom`）+ 集成测试 `form-editing.spec.jsx`。done-gate 全绿（typecheck + 前端 703 + build + workers 443 + e2e 7）。**不碰**飞书卡 link-less（PR-4）、后端（只复用现有 PATCH）。**合并 + 部署人点。**
- **两个线上修复（PR #71）**：① **DeepSeek 对话 400（模型 id 大小写）**——早期把驼峰**显示名** `DeepSeek-V4-Flash` 当 API `model` 发出去，DeepSeek OpenAI 兼容端点大小写敏感、对未知 id 直接 400，每次对话都炸。把白名单 / 默认 / 前端 `chatModels.ts` 的 **value 全改小写真 id** `deepseek-v4-flash` / `deepseek-v4-pro`（label/pill 显示名保留驼峰），并在代理发上游前 `normalizeDeepSeekModel` **归一化**（大小写不敏感匹配 + 非法回退默认）**兜住 D1 里已存的旧驼峰脏配置**——老用户不用重存。② **邮箱未验证 banner 换顶部内联条**（定稿 handoff `P9-DW3zm`）——删旧的底部浮层 `.d-verify-banner`，换成贴 app 最顶、把内容往下推的内联条 `.vb`（`.app-stack` / `.app-stack__main`）：「待验证」脉冲点 + 「验证邮件已发送至 **<email>**…」+ 带 30s 冷却的「重新发送」；账户设置页同源补 `.acct-verify` 内联卡。消费 DS `Button`，i18n 走 `L()`。契约同步 `features/chat-multi-session.feature`（区分显示名/wire id）+ SPEC §13.6 / §23.6。done-gate（typecheck + 前端单测/集成 + workers + e2e + build + prettier）+ 浏览器实测两 fix。**合并人点。**
- **全量 design-sync 复审 · 视觉对齐 `N-_ayo8x`（PR #73 / 根 A，承接 #71 的 `P9-DW3zm`；发布/分享/更新重做 + ShareDialog 拆到根 B 另起一根做）**：#67 那次 baseline 落后致多处「设计其实变了」被当已 ship 跳过 → 本票逐页复审一次扫平。① **i18n 语言切换器**（设置→账户）从 DS `Select` 下拉改 handoff `.acct-lang` **分段控件**（中文 | English + LANGUAGE 标签 + 「切换后页面会重新加载」副标，DS 无 Segmented 组件按 handoff 样式落、值引 DS token），账户字段顺序对齐（显示名→语言→邮箱）+ 登录邮箱字段右侧加验证 Badge（`.acct-emailfield`）。② **Header 语言快切**——顶栏主题/分享旁加 `IconButton`（zh 显「EN」/ en 显「中」，`setLocale` + reload）。③ **去对话标题模型 pill（⚠️ 老板拍板 deviation：`N-_ayo8x` 设计里其实有）**——DS `ConversationThread` 同一 `model` prop 同时渲染标题旁 Badge + 组合器选择器,故保留 `model`+`onModelClick`（组合器照常能选）、用精准 CSS `.ax-cthread__head > .ax-badge--outline { display:none }` **只隐藏标题旁那枚 Badge**；将来设计同步若恢复删此规则即可。④ **「我的表单」卡片对齐 `N-_ayo8x` `.mf-card`**——`src/forms-panel.jsx` 卡片从 DS `Card` 包壳改为 handoff `.mf-card` 应用层布局（folder 图标 + `/f/slug` eyebrow + 状态徽标 + 标题 + 「创建于…」+ 关闭态 `.mf-closed` 提示 + 就绪栏 `.mf-ready`「N 个正在收集 · 已关闭 N 个」），内部仍消费 DS `Badge/Button/Icon`、底部 `复制链接`+`继续编辑`+`⋯`菜单（保留 #58 的 `DropdownMenu` 收敛 + `form-publish-mgmt` 测试契约）。**recent 提交行省略**（后端 list 端点 `FormSummary` 不带 recent、只可选 `submissionCount`，渲染时 count/总数缺失即优雅省略，避免逐表 N+1 拉取——要 recent 须后端 list 带 recent，留 follow-up）。⑤ **逐页 sweep**（signin/verify/reset/public-form/submissions/preview）核对——无结构性漂移；signin/verify 的「Agentaily Forms」/「owner 账户」/ verify 成功副标比 handoff 通用文案更准 → **保留为产品专属精炼、不回退**。done-gate 全绿（typecheck + 前端 776 + workers 480 + e2e 12 + build + prettier）+ 浏览器逐页 QA（分段控件切 / header 快切 / my-forms 两态 / 标题无 pill）。**合并 + 部署人点。**
- **发布/分享/更新流程重做 + ShareDialog（PR #74 / 根 B，承接 #73 根 A 的 `N-_ayo8x`；纯前端无 D1）**：把发布从「顶栏发布 → 打开 `PublishFeedback` 浮层 → 浮层里再点一次发布」的两步,改成顶栏「**发布」直接动作**（`App.jsx` `doPublish`）——点一下直接 `POST /api/forms` 上线,成功后:① 把刚发布表单**置为「正在编辑的已发布表单」**（接上 PR-7 既有的 `editingForm`/`editBaseline`/编辑横幅/「更新」机制,顶栏主按钮即变「更新」）+ §26.2 关联 slug 进会话行（`persistTurn` 在进编辑态**之前**调,顺序敏感）② 弹**新组件 `ShareDialog`**（`src/share-dialog.jsx`,**仅链接、无二维码**,消费 DS `Dialog`/`Button`/`Icon`）publish 模式「表单已发布」庆祝式标题 + 公开链接 + 行内复制（「已复制 ✓」2s 复位、不关弹窗、clipboard 失败回退 `execCommand`）。**全程不往对话发任何消息**;后端拒绝（如缺标题 400）→ 对话外 `Alert` 横幅显后端原话、顶栏保持草稿;401→`/signin`。**分享 = 只读**（`openShare`,share 模式「分享这份表单」,仅编辑态出现,取链接不改状态不发消息）。删旧 `PublishFeedback`（`forms-panel.jsx`,连带 `.d-publish__*`）。`src/app.css` 加 `.d-share*`（等宽链接代码块）+ `.d-publish-error`。契约 `features/form-publish-mgmt.feature` 加「发布是直接动作 / 分享只读」两场景 + 发布相关集成测试改驱动真实 `<App>`；新增 `share-dialog.spec.jsx`（5 例:两模式 / 复制反馈 / 不关弹窗 / `execCommand` 回退）。done-gate 全绿（typecheck + 前端 791 + workers 480 + e2e 12 + build + prettier）+ 浏览器实测（发布→直接上线弹 ShareDialog 复制 / 关闭后顶栏「更新」常驻 / 分享只读重开浮层,截图存档）。独立 reviewer 过（publish 流程草稿/已发布/继续编辑/更新各路径无回归）。**不碰**后端 / `editbar` 内部（PR-7 HoverCard 版定稿）/ 飞书卡 link-less。**合并 + 部署人点。**
- Cloudflare Pages + GitHub Actions 自动部署（push main → 自动上线；独立 yarnbcoder 账户）

### 后端（`workers/`，Hono on Cloudflare Workers + D1）

- **MVP（PR #3）**：owner 配置存取（AES-GCM 加密）、DeepSeek LLM 代理（BYOK 流式 SSE）、连接测试、提交写飞书多维表格
- **发布闭环（PR #4）**：表单发布 `POST /api/forms`、公开拉取 `GET /api/forms/:slug`、`/api/submit` 关联 form
- **鉴权 + 数据后台（PR #5）**：`POST /api/auth/login`（密码 → session JWT）+ `requireAuth` 保护 owner-only 端点、`GET /api/forms/:slug/submissions`（读飞书提交列表）
- **补严**：CORS（白名单跨域）、提交校验（表单状态门 + 必填校验，脏 / 未发布不写飞书）、表单管理 CRUD（`GET /api/forms` 列表 · `PATCH` 改状态 · `DELETE`）、安全收尾（常量时间密码比较、字段递归深度上限、飞书探测 `res.ok`）
- **部署上线（2026-06-11）**：后端已上线 **https://form-design-api.agentaily.workers.dev** —— D1 `form-design-db`（APAC）建库 + 建表、3 个 runtime secret（`CONFIG_KEY`/`AUTH_SECRET`/`OWNER_PASSWORD`）已设、`wrangler deploy` 跑通、独立 CI `deploy-workers.yml`（push main 且 `workers/**` 变更自动部署）。独立 yarnbcoder 账户，Workers+D1 token 与运行时 secret 均进 vault。冒烟测试全绿（health 200 / 公开拉取 404 / 登录对 200 错 401）。详见 [OPERATIONS.md](./OPERATIONS.md) §12。
- **飞书列自动创建 · 自愈（SPEC §15.8）**：`POST /api/submit` 写记录遇目标表缺列（飞书 `code 1254045`）时自动补建缺失列（文本列，重复列 `1254014` 幂等）再重试一次，提交不再因「表里没有对应列」而失败；稳态（列已存在）零额外开销，不预检字段端点。owner 不必再手动在飞书表预建与表单字段同名的列。
- **多租户 / 开放注册（PR #22，已上线）**：单 owner（`OWNER_PASSWORD` + `owner_id='default'`）→ 开放注册多用户——邮箱 + 密码自助注册即成 owner（注册即登录），各自 BYOK 配置 / 表单 / 提交严格隔离。头等约束**横向越权防护**（owner-only 按 slug 操作 `WHERE … AND owner_id=?`，跨 owner → 404 不暴露存在性；公开 submit 按 slug 反查 form 所属 owner 写其飞书）。`password.ts`（PBKDF2 + per-user salt + 常量时间比对 + 防时序枚举）/ `users.ts` / `getFormOwner` / `index.ts` 接线 + 前端 `LoginDialog` 双模。inner + outer 全绿（`tenant-isolation.test.ts` / `tenant-isolation-api.test.ts` 管越权 · `auth-api.test.ts` 走 register/login · `tests/integration/owner-login.spec.jsx`）。详见 SPEC §17。
- **邮箱验证 + 找回密码（PR #26 + #28，2026-06-12 已上线 + 生产端到端验证）**：真实事务邮件走 **Resend**（纯 HTTP、无 SDK，发件域 `mail.agentaily.com` 已验证；`RESEND_API_KEY` 进 Worker secret）。**邮箱软验证**（注册即 best-effort 发验证信、点链接置 `email_verified=1`、不门禁功能；注册去重改**未验证可覆盖 / 已验证锁死**防占座）+ **找回密码**（发起永远 200 防枚举、确认凭一次性 reset token）+ `GET /api/auth/me`（banner 跨刷新拿真状态）。一次性 token 只存 SHA-256、单次、限时（verify 24h / reset 1h，新表 `auth_tokens`）。前端「忘记密码」子态 + `/reset-password` / `/verify-email` 落地页 + 未验证 banner。内外环测试 + 独立评审全过；线上验证：注册→真实验证信 Resend delivered→confirm→`emailVerified=1`。详见 SPEC §22–§24 / §17.2 / §17.12；配方沉淀为全局 skill `email-auth-resend`。
- **测试连接改按卡 · 测输入值 · 只更新本卡（PR #72，2026-06-14）**：集成设置的「测试连接」从「一个按钮测两个服务、两张卡一起变红」改为**按卡作用域**——点某张卡只测这张卡的服务（`POST /api/config/test` 新增请求体 `service: "deepseek"|"feishu"` → 响应只含该块）、用这张卡**当前输入框的值**先验后存（verify-before-save：请求体可传待测 `apiKey` / `appId`+`appSecret`，不必先保存）、**只更新这张卡**的状态。未改动的密钥前端一律 OMIT → 后端回退已存配置兜底（绝不发掩码串）；传入的待测凭据同样不落响应 / message / 日志（§14.5）。**向后兼容**：空 body / 无 `service` 仍测两块已存配置。前端 `testConnection(service, creds)` 单服务客户端 + `settings.jsx` 每卡 `onTestDeepSeek`/`onTestFeishu`，DS 卡本就有独立 status+回调、无需改 DS。契约更新 `features/integration-settings.feature` + `workers/features/conn-test.feature`，inner + outer 全绿（前端 776 + workers 480 + e2e 12 + build + typecheck）+ 独立评审（无 blocker，揪出 SPEC §14 漂移 → 同改）。SPEC §14 已重写。**合并人点。**
- **飞书列按字段类型预建 + 类型化写值（2026-06-12）**：发布 / 编辑表单时即在 owner 飞书表**按字段 `type` 预建对应类型的列**（数字 / 日期 / 单选 / 多选列,而非一律文本),owner 发布后立刻看到完整且类型正确的结构;**best-effort**(未配飞书 / 连不上 / 建列失败 → 发布仍 `201`、`waitUntil` 后台静默跳过、只记 `err.name` 不记凭据)。提交按列**真实**类型格式化写值(方案 a:先 `listBitableColumns`);§15.8 自愈升级为「按字段 type 建对应类型列」兜底(且缺列按字段映射类型格式化值、摊平 `group` 子字段)。单一真相源 `FIELD_TYPE_TO_BITABLE`。inner + outer 全绿(`feishu-schema.test.ts` + `feishu-typed-columns-api.test.ts` 18 场景)+ 独立评审(揪出并修了 2 个自愈类型/分组 bug)。SPEC §16.8 / §15.8 升级。
- **公开端点限流 / 防刷（2026-06-12）**：4 个公开端点(`POST /api/submit` / `register` / `login` / `password-reset/request`)挂 Cloudflare **KV 固定窗口** per-IP 限流,超限 `429 + Retry-After`,**KV 故障 fail-open**(限流器自己挂了不拖垮正常请求)。护 owner 飞书写额度 + 共享 Resend 免费档(防被刷爆→全员收不到验证/重置信)+ 登录防爆破。键只存 `SHA256(ip)`(不存原始 IP);限额 submit 10/min·100/h、register 5/h、pwreset 4/h、login 10/min;owner-only / `/health` / OPTIONS 不限(§11 各烧自己额度的不限流)。inner + outer 全绿(`ratelimit.test.ts` + `rate-limit-api.test.ts`,证明「429 = handler 不执行」)+ 独立评审 ship。**线上验证**:连打 login → 429 触发(20 连打 10×429)。SPEC §25。**已知局限(best-effort,非铁壁)**:① KV 最终一致性 + 读缓存,连打时会「漏到约 2 倍配额」才挡(SPEC §25.2 容差;miniflare 强一致所以测试没暴露);② per-IP 挡得住「单 IP 一键脚本」,但挡不住分布式/耐心(register 5/h/**IP** × 多 IP / 多时段仍可超 Resend 100/天)。**follow-up / 加固**见待办。
- **改字段标签 → 同步飞书列改名（2026-06-12）**：owner 在设计器改某字段 `label` 再保存 → 系统去飞书把**那一列改名**(而非按新标签新建一列、把旧列连同已收数据丢下/分家)。定位靠字段稳定 `id` 配对 + 旧 `label` 即列名(不建持久映射表);编辑路径 `waitUntil` 里**先改名后预建**(改名后预建看到列已存在即跳过、不重复建);best-effort(未配飞书 / 连不上 / 改名失败 → 编辑仍 `200` 静默跳过、不记凭据)。**v1 只改名**:删字段 → 列保留不动(**绝不删已收数据**)、改类型 → 列不动(§16.8.4 兜值)、排序不同步。inner(`computeFieldRenames` 含同 label / 分组 edge)+ outer(`feishu-column-rename-api.test.ts` 17 场景)全绿 + 独立评审 ship(揪出并修了「旧 schema 同 label 字段漏判改名→丢数据」edge)。SPEC §16.8.7。**follow-up**:删列 / 改类型 / 排序同步、字段 `id`↔飞书 `field_id` 持久映射(比靠旧 label 更扛漂移)、改名+预建合用一次 token/list(现各读一次)。
- **飞书 per-form 动态建表（发布即建表，PR-3 / #59）**：飞书从「owner 单一对 `app_token`/`table_id`、全平台表单挤一张表」升级为 **per-form 一张飞书多维表格**。owner 只配**账户级**凭据（`app_id` + `app_secret`，`saveConfig` 放宽、不再要求粘 token）；发布 `POST /api/forms` 成功后**后台 best-effort 自动建表**——换 token → 建多维表格 app → 建数据表 → 把 `app_token`/`table_id` 回写进**该 form 行**（migration `0006_form_feishu_table.sql` 给 `forms` 加两列，可空成对）→ 复用 §16.8 逻辑预建带类型的列。**best-effort 铁律**：未配飞书 / 建 app / 建 table / 建列任一失败都不挡发布（表单照常 `201 published`、form 行飞书两列保持 `NULL`）。提交同步 / 编辑预建 / 改名改读 `getFormFeishuTable(slug)` 的 per-form 表（没建表则跳过）；**干净起步零回填**（只新发布表单走自动建表）。新函数 `createBitableApp`/`createBitableTable`/`ensureFeishuTableForFormBestEffort`（`workers/src/feishu-schema.ts`）。owner_config 旧两列保留但对同步弃用（仅回显，PR-4 清理）。inner + outer 全绿（workers 442 + 前端 655）。SPEC [§16.9](./SPEC.md) + §12.1 / §15 改读。**前端飞书卡 link→link-less 是 PR-4。**
- **飞书卡 link-less + per-form「飞书表格↗」入口 + 清 echo（PR-4 / #61，承接 #55+#59）**：① **飞书卡 link-less**——集成设置的飞书卡去掉「多维表格分享链接」输入（连同内联 `parseFeishuLink`/`linkFromStored` 与解析回显行），改成与 DeepSeek **对称**的「App ID + App Secret + 测试连接」（消费 DS `ConnectionCard`+`SecretField`+`HelpSteps`），保存只发 `app_id`+`app_secret`，`HelpSteps` 末步改「连接后由 Agentaily 自动建表」。② **per-form「飞书表格↗」入口**——「我的表单」→「提交数据」工具栏（挨着「导出 CSV」）新增外链：该表单已自动建表（`forms` 行 `feishu_app_token`/`table_id` 都非空）时显示，新标签打开**这张表单对应的**飞书多维表格；`GET /api/forms` 投影 `forms` 行 per-form 表定位（`FormSummary.feishuTable`，两列都非空才带），前端纯函数 `feishuTableUrl(appToken,tableId)` 拼 URL，**不**来自提交响应（§18.6）。DS Button 撑不起新标签链接，按 chat13 退化为复用 `ax-btn` 样式的真 `<a>`。③ **echo 清理（接 #59 推迟）**——`MaskedConfig.feishu`/`FeishuInput` 去 `appToken`/`tableId`，`getMaskedConfig` 不再回显、`saveConfig` 把 owner_config 两列恒绑 `NULL`（列保留向后兼容）；`getOwnerConfig`/同步链不动（读 `getFormFeishuTable` + `owner.feishu` app_id/app_secret 换 token，不受影响）。契约：`integration-settings.feature` 去 app_token/table_id 回显 + 删「半填→400」场景；`data-dashboard.feature` 加 2 条 per-form 入口场景；SPEC §12/§21 + DESIGN 同步。spec-architect→implementer→outer-tester→reviewer（独立审，无 blocker，揪出 worktree 基于 #63 前 stale base → 已 rebase 引入 #63 + DS ^0.10.1）。done-gate 全绿（typecheck + 前端 662 + build + workers 445 + e2e 7）。**合并 + 部署人点。**
- **提交主存翻转到 D1，飞书降为可选同步（PR-2 / #56，架构转向）**：提交数据的**主存**从「owner 的飞书多维表格」翻转到 **D1**。`POST /api/submit` 校验门(form 存在 + 状态开放 + 必填)全过后**先写 D1 `submissions` 主存(必成)→ 返回 `{ ok, id }`**;飞书降为**可选外部同步出口**——仅当 form 所属 owner 配了飞书,才在 `waitUntil` 后台 **best-effort 同步**一条记录(复用换 token + 类型化写值 + §15.8 自愈),成功回填 `feishu_record_id`、失败只记 `feishu_sync_error`,**绝不影响提交成功**。**语义翻转:未配飞书不再 409、照常落 D1**;`GET /api/forms/:slug/submissions` 改**从 D1 按 (owner_id, form_slug) 读**(不再读飞书、无 409/502 上游分支),投影 `{ id, answers, createdAt, feishu }`。新 migration `0005_submissions.sql`(id/form_slug/owner_id/answers_json/created_at + 飞书回执三列 + 复合索引)。**干净起步、零回填**(已与负责人确认,生产自测态)。翻转 submit/submit-validation/submissions 三 feature 的 409 断言 + 新增 `submissions-storage.feature`;inner(`submissions.test.ts` D1 store)+ outer(submit / submissions / submit-validation / submit-autocols / feishu-typed-columns / tenant-isolation 全部改飞书同步为后台 drain + 新 `submissions-storage-api.test.ts`)全绿(workers 430 + 前端 655 + e2e 7)。SPEC §15 / §18 已重写。**边界**:不碰前端面板/数据后台 UI(PR-6)——`src/core/submissionsClient.ts` / `publicClient.ts` 的响应类型待 PR-6 对齐 D1 投影。**合并 + 部署(含 D1 迁移上生产,不可逆)人点。**
- **A'「项目 ↔ 对话」模型重构(4 根有序 PR · #81/#83/#84/#85,2026-06-15 全部上线)**：给「会话」上面加「项目」层——一个**项目** = 一份表单的容器，承载项目级**共享**工作区(`meta` + `fields`)，其下多条会话共编同一份表单(切对话只换左侧聊天线索、右侧工作区不变)，纠正 #76「工作区快照骑在每条会话 `turns_json`」的反直觉做法。**PR-A(#81)** 后端契约 + `0007` migration(新 `projects` 表 + `chat_sessions` 加可空 `project_id`/`title` + owner-only `GET/PUT/DELETE /api/projects*` + `PATCH /api/chat/session/:id` rename,纯加性灰度)；**PR-B(#83)** 一次性数据迁移(owner-only `POST /api/admin/migrate-projects`,把老会话迁成单对话项目、抽 #76 快照写 `projects`,先备份可回滚,生产由老板手动 dry-run→apply,确认**当前生产无老数据可迁**、`migrated:0` no-op)；**PR-C(#84,最关键一根)** 前端真正切到 A'(新 `src/core/projectClient.ts` + 对话 project-scoped + 工作区项目级 + URL `/p/:id?s=` + 对话标题可编辑 + 删 #76 快照符号块)，并钉死修复**两个线上 bug**——①「继续编辑崩/黑屏」(`preview.jsx` `FieldView` 选择字段缺 `options` 默认 `[]`)②「刷新丢对话」(去掉 `editingFormRef` 跳过持久化的语义)；**PR-D(#85)** 收口清债——删一次性迁移端点 + `migrateProjects.ts` + 两测试、新 `0008` migration `DROP TABLE chat_sessions_a_backup`、清 #76 快照符号最后引用、runbook 标完结。done-gate 全绿、零回归。方案母本见 [docs/refactor-project-conversation.md](./docs/refactor-project-conversation.md);契约 SPEC [§26 / §26.10](./SPEC.md)。
- **未登录守卫 · 设计器入口级守卫(PR #90 / design-sync 落地 `_QB7NM8v` 单页 SPA + auth 守卫,生产 auth 改动)**：把设计器从「整页渲染 + 按动作 `guard()` 拦截」升级为**入口级单层客户端守卫**——设计器现挂在新 `src/auth-gate.jsx` 的 `<AuthGate>` 之后,进入前先跑**真实** `GET /api/auth/me`(新 `core/auth.validateSession` 返回判别式 `authed | unauthed | error`,区分 401↔5xx/网络,补 `getCurrentUser` fail-soft 合并 null 的盲点),据结果路由:已登录 → 设计器;**未登录 / 会话失效 → 原地整屏登录视图**(复用 `<SignInScreen>`,登录成功原地重校验、不跳转白屏);**5xx / 网络 → 中性「重试 / 去登录」占位**(不把异常误判为未登录);校验中 >200ms 才显现品牌占位(极快校验直接落地)。受保护内容**仅在校验通过后才实例化**(零闪烁:未授权用户拿不到设计器任何可见帧)。**「待验证」保持软提醒**(200 未验证仍进设计器 + 顶部 amber 提醒条,不硬墙,Q2 定稿)。落地为 **pixel-perfect 设计 + idiomatic React**:消费 DS `BrandMark`/`Button`、`.ag-*` 布局样式引 DS token、**排除原型设计工具层**(`_mock-backend.js` / design-kit CDN `installMockBackend` / `GuardDemoPicker` Tweaks 场景切换器 / web-kit i18n 这次缓);守卫**纯加法**——会话中途 401 仍由 `DesignerApp` 的 `guard()`/`needLogin` 兜底(入口已保证登录 → happy path 直通)。契约新增 [`features/auth-gate.feature`](./features/auth-gate.feature)(四态 + 零闪烁)+ `tests/integration/auth-gate.spec.jsx` + `core/auth.validateSession` 单测 + `AuthGate` 组件单测;`owner-login.feature` 的「未登录引导」场景重定义为**会话中途失效**引导。SPEC [§17 / §23.6](./SPEC.md)。**ready 不合**:动线上登录 / 路由,worker push + done-gate 绿,老板 review + 合 + 部署。
- **主题持久化 · 接 `@agentaily/web-kit` ThemeProvider(PR #91,修线上 bug:设计器切主题刷新回落 dark)**：设计器主题原是 `useUiState(UI_DEFAULTS)` 的纯内存态(默认硬编码 dark、零持久化),刷新即丢。本票把主题从 `UI_DEFAULTS` 摘出,改由 **web-kit `<ThemeProvider defaultTheme="dark">`** 拥有——跨子域 cookie `agentaily:theme`(`domain=.agentaily.com`,localhost 自动回退 localStorage)持久化,与营销站(form-design-website,已正确)**共用同一存储键 + 同一默认**,互相跟随;顶栏主题钮改走 `useTheme()` 的 `resolvedTheme`/`setTheme` 二态切换,删本地 `data-theme` useEffect(ThemeProvider 接管)。**防 FOUC**:`vite.config` 用 `themeInitScript({defaultTheme:"dark"})` 在 `<head>` 顶部注入阻塞内联脚本(从 lib 生成、单一真相源,与营销站线上 FOUC 脚本**逐字节相等**),首屏即正确主题。只动 theme——split/density/formStyle 仍是固定产品默认不变。**范围隔离**:web-kit i18n(`createI18n`)是另一张工单(#89),本票不碰。契约 [`features/theme-persistence.feature`](./features/theme-persistence.feature) + `tests/integration/theme-persistence.spec.jsx`。done-gate 全绿(typecheck + 前端 904 + build + prettier)。**ready 不合**:纯前端 CF Pages、动线上 UI 行为,worker push + done-gate 绿,老板 review + 合 + 部署。

> 后端全程双循环 TDD（spec → outer → impl → review），凭据进 vault（DeepSeek key、飞书自建应用、CF token、runtime secret、Resend key）。

---

## 🚧 进行中

- **设计对话持久化 + 刷新恢复（PR #48，绑账号）**：把设计器左侧对话（UI 回合 `messages` + LLM 历史 `historyRef`，本只活在浏览器内存、刷新即丢）随聊天写进后端 D1，登录态重载 / 换设备时按原顺序恢复、可继续往下聊。会话按【客户端生成、localStorage 持久化的稳定 `designSessionId`】绑定，键 = `(owner_id, session_id)`（发布前没稳定表单 id，故不按表单 keying；发布只把 slug 关联进会话行，id 不变）。未登录 = 不持久化（沿用现状 401→/signin）；写入按**回合结束批量**整段 `PUT`（绝不每 token）。新增 D1 表 `chat_sessions`（migration `0003`，复合主键）+ owner-only `GET/PUT /api/chat/session/:sessionId`。**已完成**：前端 client（`src/core/chatSessionClient.ts`：`getOrCreateDesignSessionId` / `loadChatSession` / `saveChatTurns` / `toPersistedTurns`）+ App 接线（`src/App.jsx`：登录态 load-on-mount 恢复两份转写、turn-end best-effort save、发布关联 slug）+ 后端（`workers/src/chatSessions.ts` + 路由）；内环单测 + workers API 测试全绿。**待**：outer-tester 的 integration/e2e realize（`features/chat-session-persistence.feature`）+ 评审 + CI 绿后合。SPEC [§26](./SPEC.md)。
- **多会话列表 + 删除 + 对话级模型芯片（PR #65，承接 #48）**：把 #48 单会话持久化升级为「一个 owner 多段对话并存」。新增 owner-only `GET /api/chat/sessions`（列出当前 owner 全部会话摘要 `ChatSessionSummary[]`，按 `updated_at DESC`，零会话 → `{ sessions: [] }`）+ `DELETE /api/chat/session/:sessionId`（删到 → `200 { deleted: true }`，无匹配行 / 属于别的 owner → **404** `会话不存在`，owner 隔离）。列表项 `title`（首条 user 消息 trim+截断 40 字，无则「新会话」）/ `turnCount`（user 回合数）**运行期从 `turns_json` 推**——**不加 D1 migration**（0003 的 `idx_chat_sessions_owner` 索引已预留）。新建 = 换新 `designSessionId` 清空工作区；切换 = 活跃键指向某 sessionId 后复用 `loadChatSession` 载回（前端编排，无新端点）。**对话级模型(型号)选择器**（§13.6）：`POST /api/chat` 加可选白名单 `model`（`DEEPSEEK_MODELS = [DeepSeek-V4-Flash, DeepSeek-V4-Pro]`，旧 deepseek-chat/reasoner 作废），取值优先级 `request.model || owner.deepseek.model || 默认 DeepSeek-V4-Flash`，非白名单 → `400 unsupported model`；前端选项 / 校验在 `src/core/chatModels.ts`，选择只作 UI 偏好持久化、不当凭据。收起 pill 显示当前型号（默认「DeepSeek · V4-Flash」）、点开两项下拉，放在 composer（manager 最终 spec from-parent-2）。**已实现**：契约（SPEC §26.9 / §13.6、`features/chat-multi-session.feature`）+ 后端（`workers/src/chatSessions.ts` 列表/删除 + `workers/src/chat.ts` 白名单 + 路由 + 代理 per-request model）+ 前端（`src/core/chatSessionClient.ts` 列表/删除 + active-id helper、`src/core/chatModels.ts`、`src/SessionMenu.jsx`、App 接线 + 模型选择器）；内环单测 + workers API 测 + 集成 + e2e 全绿，独立评审过。**待**：CI 绿 + 人合。**DS seam（已闭环，PR #66）**：原 #65 靠 wrapper 截 `.ax-composer__model` 内部类让模型 pill 可点（DS 0.10.1 不透传 `onModelClick`）；DS **0.11.0** 已上游加 `ConversationThread → Composer` 的 `onModelClick` 透传，PR #66 撤掉该 wrapper 垫、改用官方 prop（弹层锚定改纯 CSS）。SPEC [§26.9](./SPEC.md) / §13.6。
- **URL 状态持久化 · 会话/设置tab 进 URL + 刷新完整恢复对话+工作区（PR #76，承接 #48/#65，纯前端无 D1）**：把 app 内「有状态的视图」反映进 URL，刷新/深链/分享/前进后退都落回同一状态。**① 设计会话进 URL**（`?s=<designSessionId>`，与 §26.2 同值，与 `/settings/:tab` 路径正交）：进入设计器即把会话规整进 URL（replaceState）；带 `?s=` 刷新 → `setActiveDesignSessionId` + load 该会话 + **完整恢复对话 AND 工作区**；新建/切换会话改 URL（pushState，故后退可回上一段会话）；popstate 切回对应会话且**绝不串会话**（`switchSession` 载入并 apply 目标会话自己的转写 + **无快照即复位空模型**，修掉切换时上一段表单残留的潜在 bug）；非法/未持久化 `?s=` → 退化空态不报错。**② 设置 tab 进 URL**（`/settings/account` | `/settings/integrations`，裸 `/settings` 退化默认集成 tab，未知段→设计器）：打开/切 tab 反映进路径（保留 `?s=`）、深链/刷新落回对应 tab、关闭回设计器仍带会话。**③ 飞书表选择进 URL：当前无可落 UI**——DS 0.10.0/§16.9 后飞书集成只配 App ID+Secret、表格发布时 per-form 自动建（无「切表」侧边栏），故本票不含（已在 feature/PR 说明）。**工作区恢复机制（无 D1，"接 #48"）**：§26 只持久化对话（turns+history），表单预览模型靠回放 tool 调用重建不可靠（uid 与消息 id 交错→字段 id 引用错位），故把模型快照（meta+fields）作为一条**合成 turn**（role:`assistant`、哨兵 id）搭进 §26 既有的**不透明 `turns_json`**（后端零改、不影响按 user 回合推的 title/turnCount，恢复时滤掉不渲染成气泡）。**落点**：`src/core/router.ts`（`readSessionId`/`withSessionId`/`SESSION_PARAM` + `matchSettings` 返 `{section}` + `settingsPath`）+ `src/core/chatSessionClient.ts`（`WORKSPACE_SNAPSHOT_ID`/`buildWorkspaceSnapshotTurn`/`splitWorkspaceSnapshot`）+ `src/App.jsx` 接线。契约 `features/url-state-persistence.feature`；内环单测（router + 快照纯函数）+ 外环（`tests/integration/url-state-persistence.spec.jsx` + `e2e/url-state-persistence.spec.js`）。done-gate 全绿（typecheck + 前端 822 + workers 480 + e2e 14 + build + prettier）。**待**：独立 reviewer + CI 绿 + 人合。
- 上一批后端能力（飞书列按类型预建 / **改字段标签同步** / 公开端点限流）均已上线（见「已完成」）。
- **下一步候选（待办）**：飞书增强剩余（`listBitableColumns` >100 列分页、list 故障时降级到无类型写值而非 502）、数据后台增强（CSV 导出 / 分页 / 统计；按 `formSlug` 过滤已随 D1 主存翻转解决 ✅，见「已完成」PR-2）、**限流加固**（小时窗 429 外环补测、双窗 `Retry-After` 取较大值;**Durable Objects 替 KV** 求强一致、精确无漏限流；**全局 Resend 日发送封顶** —— 无论多少 IP，当天总发送够 N 封即停，直接护住 100/天那条线，补 per-IP 挡不住分布式的缺口）。

---

## 📋 待办

### 后端

- 绑自定义域名 `api.form-design.agentaily.com`（可选；现走 `*.workers.dev` 默认域）
- **表单新提交通知（未来，先不做 · 以后再说）**：owner 的表单收到新回复时给 owner 发邮件通知。**必须按天聚合 + 每表单可开关**（默认关）——「每条提交即发」量随访客流量不可控、会冲破 Resend 免费档变贵；按天聚合后量级 = 活跃表单数 × 天，基本恒在免费档内。复用已上线的 Resend 发信基建（见「已完成」邮箱验证条 / 全局 skill `email-auth-resend`），改动落在公开 submit 路径，需配通知开关等产品决策，故单独一轮做。
- 数据后台增强：聚合统计、分页、CSV 导出

### 飞书端到端

- ✅ **端到端已打通（2026-06-12）**：应用用自身 `tenant_access_token` 自建多维表格（绕开「纯 API 应用加不进已有表协作者」的墙）→ `app_token`/`table_id` 配进 `/api/config`；`设计→发布→公开填写→写飞书→数据后台` 全链路在生产验证通过（含真实 UI 填写）。
- ✅ **提交自愈建列**（见上「已完成 · 后端」）——去掉了「owner 须手动预建同名列」这一步。
- ✅ **发布即预建列 + 列类型精确映射（2026-06-12）**：发布 / 编辑表单时即在飞书表按字段 `type` 预建对应类型列（数字 / 单选 / 日期 / 多选列而非一律文本）+ 提交按列真实类型写值 + 自愈升级。见「已完成 · 后端」；SPEC [§16.8](./SPEC.md) + [§15.8 升级](./SPEC.md)。
- ✅ **提交主存翻转到 D1（2026-06-14，PR-2）**：提交先落 D1 主存、`/submissions` 从 D1 按 `(owner_id, form_slug)` 读（天然按表单过滤、owner 隔离，不再读整张飞书表会混），飞书降为 best-effort 可选同步。见「已完成 · 后端」；SPEC [§15](./SPEC.md) / [§18](./SPEC.md)。
- ✅ **发布即自动建表 · per-form 一张飞书表（PR-3 / #59）**：owner 只配账户级 `app_id`+`app_secret`，发布即自动建一张 per-form 多维表格（建 app + 建表 + 回写 form 行 + 预建列），不再手动建表 / 粘 token；提交同步 / 编辑预建 / 改名改读 per-form 表。见「已完成 · 后端」；SPEC [§16.9](./SPEC.md)。

### 前端接入后端（`src/`，分阶段）

- ✅ API client 层 + 对话接 `POST /api/chat`（真模型流式替换写死脚本）—— 已完成
- ✅ 登录接 `POST /api/auth/login`：`core/auth` 存 session token、owner-only 请求带 Bearer；登录 / 账户弹窗（DS），`401` 自动引导登录 —— **已完成（解锁 `/api/chat`）**
- ✅ 找回密码 + 邮箱验证前端：`LoginDialog`「忘记密码？」子态（中性防枚举）、`/reset-password?token=` 改密落地页、`/verify-email?status=` 结果落地页、设计器「邮箱未验证 · 重新发送」banner（依 `GET /api/auth/me` 真状态、跨刷新权威）—— **已完成**
- ✅ 集成设置 modal 接 `GET/POST /api/config` + `POST /api/config/test`：`settings.jsx`（掩码回显 / 不重交密文 §12.4 / 401 引导登录 / 逐条测连接）+ `core/configClient.ts` —— **已完成**（`integration-settings.spec.jsx` / `app-settings-login.spec.jsx`）
- ✅ 发布 + 表单管理：`forms-panel.jsx` + `PublishFeedback` + `core/formsClient.ts`（发布 `POST /api/forms`、列表 / 改状态 / 删 `GET/PATCH/DELETE /api/forms`）—— **已完成**（`form-publish-mgmt.spec.jsx` / `app-forms-login.spec.jsx`）
- ✅ 公开填写页 + 数据后台：`public-form.jsx`（`/f/:slug` 渲染 + `POST /api/submit`，`core/publicClient.ts`，不带 Bearer）+ `submissions-view.jsx`（`GET /api/forms/:slug/submissions`，`core/submissionsClient.ts`）—— **已完成**（`public-fill.spec.jsx` / `fill-and-submit.spec.jsx` / `data-dashboard.spec.jsx`）
- ✅ **i18n 国际化（en/zh，PR #67 / design-sync `P9-DW3zm`）**：极简双语层 `src/core/i18n.ts` —— 调用点翻译 `L("中文","English")`（无字典不漂移）、locale 读 `localStorage agentaily.locale.v1`（默认 zh、加载即缓存、`<html lang>` 反映）、`setLocale` 写入 + `location.reload()` 应用。把 12 个 `src/*.jsx` 的硬编码中文显示串逐个包成 `L(zh,en)`（**zh 逐字不变 → 非破坏，断言中文的现有测试全绿**），设置「账户」tab 加 DS `Select` 语言切换器（owner 受众）。TDD（`tests/unit/i18n.test.js` + en 渲染冒烟 `i18n-render.test.jsx`）。**已知小缺口**：`core/markup.ts` 字段类型标签（指向修改工具）暂留 zh；登录前页面（signin/verify）无切换入口（主切换器在设置）。
- ✅ **邮箱验证落地页接 DS 0.12.0 `VerifyEmailPage`（PR #67）**：`/verify-email` 从手搓 Empty/Button 垫片改为消费官方 DS 组件（bump `@agentaily/design-system ^0.11.0→^0.12.0`）。后端 server 端 confirm + 302 带 `?status=` 的架构**不变**（§23.4），故组件走 **controlled 模式**（传 `status`/`error` 展示已决 verdict、零后端契约变更），注入 `onResend`（owner-only `requestEmailVerification`）/ `returnTo` / `onContinue`（内部回设计器，无开放重定向）/ `onBackToSignIn` + 全 i18n `copy`；硬约束（失败不自动跳 / 成功倒计时 / 重发冷却 + 幂等 +「重发≠已验证」）由组件保证。`verify-email.test.jsx` 重写。

### 产品（SPEC §10 Phase 4）

- 发布态渲染：固定 schema 渲染器（默认）/ 发布时编译快照
- 公开填写页：防刷、防注入、提交落库可视化

---

_进度以各 PR 为准；架构决策见 SPEC.md 对应章节（§12–§21 为后端）。_
