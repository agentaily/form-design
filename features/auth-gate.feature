Feature: 未登录守卫(设计器入口)
  作为表单作者
  我希望设计器是一个受保护页:进入前先校验登录态
  以便未登录 / 会话失效时直接、无闪烁地落到登录视图,绝不先把设计器渲染出来给未授权用户

  背景(设计源 _QB7NM8v · SPEC §17/§23.6):设计器挂在单层客户端入口守卫 <AuthGate> 之后。进入受保护
  页 → 先跑真实会话校验(GET /api/auth/me)→ 据结果路由:已登录有效 → 进设计器;未登录 / 会话失效 →
  原地整屏登录视图(不跳转白屏);校验服务异常(5xx / 网络)→ 中性「重试」占位。受保护内容只有在
  校验通过后才实例化(零闪烁:未授权用户永远拿不到设计器的任何可见帧或可点控件)。「待验证」是软提醒
  (200 未验证仍进设计器 + 顶部提醒条),不是硬墙。

  Scenario: 已登录有效则进入设计器
    Given owner 的会话校验通过(已登录)
    When 进入设计器入口
    Then 守卫挂载设计器
    And 不显示登录视图

  Scenario: 未登录则原地落到登录视图,绝不渲染设计器
    Given owner 未登录(会话校验判定未授权)
    When 进入设计器入口
    Then 守卫原地展示登录视图
    And 绝不渲染设计器的任何受保护内容

  Scenario: 校验服务异常时给中性「重试」占位而非登录视图
    Given 会话校验服务异常(5xx / 网络)
    When 进入设计器入口
    Then 守卫展示中性「重试」占位
    And 不把异常误判为未登录(不直接落登录视图)

  Scenario: 校验异常后重试成功则进入设计器
    Given 首次会话校验异常、重试将通过
    When 进入设计器入口并点击「重试」
    Then 守卫重新校验并挂载设计器

  Scenario: 待验证邮箱仍进入设计器并显示软提醒条
    Given owner 已登录但邮箱待验证
    When 进入设计器入口
    Then 守卫挂载设计器
    And 顶部显示邮箱未验证的软提醒条
