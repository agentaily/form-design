name = "form-design-api"
main = "src/index.ts"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# D1 — created later with `wrangler d1 create form-design`; wire the binding here
# once it exists (the owner-config feature will need it).
# [[d1_databases]]
# binding = "DB"
# database_name = "form-design"
# database_id = "<filled after create>"

# CONFIG_KEY (AES-GCM master key for encrypting owner secrets) is set via
# `wrangler secret put CONFIG_KEY`, never committed.
