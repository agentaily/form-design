name = "form-design-api"
main = "src/index.ts"
compatibility_date = "2026-06-01"
compatibility_flags = ["nodejs_compat"]

# D1 — the owner-config feature needs it. Provisioned in prod via
# `wrangler d1 create form-design-db` (yarnbcoder account, APAC). Under
# vitest-pool-workers the binding runs against a local miniflare D1, so the id
# only matters for real deploys. The Worker reads it as `env.DB`.
[[d1_databases]]
binding = "DB"
database_name = "form-design-db"
database_id = "9666fb73-cf26-471c-b159-a08e42f94762"
# Schema 演进走 wrangler D1 迁移：migrations/*.sql 由 `wrangler d1 migrations apply`
# 按序、幂等应用（追踪表 d1_migrations）。deploy-workers.yml 在每次部署前自动 apply。
# 一次性数据 backfill 不在此目录（见 runbooks/，手动 runbook）。
migrations_dir = "migrations"

# KV — 公开端点限流 / 防刷的固定窗口计数器（§25）。Worker 读它为 `env.RATE_LIMIT`
# （ratelimit.ts 的 rateLimit 中间件）；键 = hash(ip)+bucket+windowStart，值是计数、
# TTL = 窗口长度（到期自动清，§25.2）。**只**服务公开端点限流（submit / register /
# pwreset / login）；owner-only 与 /health 不读它。
#   * prod：`wrangler kv namespace create RATE_LIMIT`（yarnbcoder 账户）拿到真实 id 后
#     由 release-eng 填进下面的占位 id；schema-less，无需迁移。
#   * 测试：vitest-pool-workers 下 binding 跑在本地 miniflare KV，id 仅对真实部署有意义——
#     vitest.config.ts 的 cloudflareTest 接 wrangler.toml 的 kv_namespaces，自动提供本地 KV，
#     implementer 无需在 miniflare.bindings 里手注 binding 值。
[[kv_namespaces]]
binding = "RATE_LIMIT"
id = "c0deecf24a7446fb8a3ad196ce853133" # form-design RATE_LIMIT KV（yarnbcoder 账户）。本地测试 miniflare 自动按本 binding 提供本地 KV。

# CONFIG_KEY (AES-GCM master key for encrypting owner secrets) is set via
# `wrangler secret put CONFIG_KEY` in prod, never committed. For tests it's
# injected as a miniflare binding in vitest.config.ts (a throwaway 256-bit key).

# 发信抽象 (§22) 的非密配置 —— 明文 vars（非敏感，可入 git）：
#   * EMAIL_FROM  : Resend 的发件人（mail.agentaily.com 发件域已验证，§22.1）。
#   * APP_BASE_URL: 前端站点根，拼邮件里的落地页链接（验证确认 / 找回密码，§22.1 / §23.3 / §24.2）。
# RESEND_API_KEY 是 **secret**，**不**写进本文件——生产 `wrangler secret put RESEND_API_KEY`
# （release-eng 负责），测试由 vitest.config.ts 注入 throwaway 值（不真发信，外环 mock fetch）。
[vars]
EMAIL_FROM = "Agentaily Forms <noreply@mail.agentaily.com>"
APP_BASE_URL = "https://form-design.studio.agentaily.com"
